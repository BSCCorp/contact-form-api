contact-form-api
================

This a generic contact form API with a demo testing with `playwright`.


# Setup

Clone and install:

```
cp .env.example .env
npm install
```

For testing and development, start a MongoDB container:

```
docker run --name dev-mongo -p 27017:27017 -d mongo
```

